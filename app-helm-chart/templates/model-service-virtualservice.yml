apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ .Values.modelService.dns-name }}-vs
spec:
  hosts:
    - {{ .Values.modelService.dns-name }}
  http:
    - match:
        - sourceLabels:
            app: app-service
            version: {{ .Values.appService.canary.versionLabel }}
      route:
        - destination:
            host: {{ .Values.modelService.dns-name }}
            subset: {{ .Values.modelService.canary.versionLabel }}
    - route:
        - destination:
            host: {{ .Values.modelService.dns-name }}
            subset: {{ .Values.modelService.stable.versionLabel }}